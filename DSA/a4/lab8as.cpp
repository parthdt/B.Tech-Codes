#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>

#define Max(a, b) ((a) > (b) ? (a) : (b))

using namespace std;

struct Node 
{
	int height,data;
	Node *right,*left;
};

Node *CNN(
int main()
{

	return 0;
}


